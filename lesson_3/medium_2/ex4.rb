puts "My string looks like this now: pumpkinsrutabaga"
puts "My array looks like this now: [pumpkins]"

=begin
  
+= doesn't mutate the input parameter, and since assignment happens inside a method
<< mutates the actual array, so even though object id doesn't change, the object itself does

=end